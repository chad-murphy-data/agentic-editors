services:
  - type: web
    name: document-refinement
    runtime: node
    rootDir: document-refinement
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
